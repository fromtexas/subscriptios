import styled from "styled-components";
import { black } from "../variables";

export const SectionTitle = styled.h4`
  color: ${black};
  font-size: 1.8rem;
  font-weight: bold;
  width: 100%;
  grid-column: 1/-1;
`;
