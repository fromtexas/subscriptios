import styled from "styled-components";
import { lightSlateGray, darkSlateGray } from "../variables";

export const Footer = styled.footer`
  margin-top: 3rem;
  display: flex;
  border-top: 2px solid ${lightSlateGray};
  height: 8rem;
  align-items: center;
  color: ${darkSlateGray};
`;
